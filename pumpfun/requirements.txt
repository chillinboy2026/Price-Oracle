# Core pipeline
pydantic>=2.7,<3
PyYAML>=6.0
httpx>=0.27
websockets>=12.0

# Live execution on Solana. Only imported when mode: live -- the pipeline
# runs dry-run without these installed.
solders>=0.21
base58>=2.1
