{
  "name": "@price-oracle/offchain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "tsc -p tsconfig.check.json && vitest run",
    "dev": "tsx src/index.ts",
    "demo": "tsx scripts/demo-live-feed.ts",
    "demo:preipo": "tsx scripts/demo-preipo.ts",
    "demo:comps": "tsx scripts/demo-comps.ts"
  },
  "dependencies": {
    "ethers": "^6.13.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "vitest": "^2.0.5",
    "@types/node": "^20.14.10"
  }
}
